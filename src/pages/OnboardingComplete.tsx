const OnboardingComplete = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-white to-gray-50 flex items-center justify-center">
      {/* Blank page - to be designed later */}
    </div>
  );
};

export default OnboardingComplete;
